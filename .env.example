# Local development only. Copy to .env.local and supply non-production values.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Optional same-origin public archive; blank keeps the source-free default.
REALITY_LEDGER_PMTILES_PATH=
# Intentionally blank: live Overpass ingestion fails closed until an approved public URL is set.
REALITY_LEDGER_OVERPASS_CONTACT_URL=
